apiVersion: recluster.io/v1
kind: RcNode
metadata:
  labels:
    app.kubernetes.io/name: recluster4
    app.kubernetes.io/managed-by: kustomize
  name: rcnode-sample
spec:
  # TODO(user): Add fields here
